{
  "//": "The end-to-end runs in Node and drives a browser, so it needs both sets of globals. It is a separate project from tsconfig.json so that `src` keeps its browser-only type environment: nothing shipped to the page should typecheck against `node:fs`.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["e2e", "playwright.config.ts"]
}
